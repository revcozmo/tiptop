This folder is a dummy. If there's no history folder in the application storage on the device yet, then getPersonalisedData() looks for ths folder in the root directory.
In order to keep the code simple this directory contains .json files with the content "false".
